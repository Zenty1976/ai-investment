READ-ONLY DIAGNOSTIC — DO NOT CHANGE CODE

Do not implement anything.
Do not refactor anything.
Do not run OpenAI.
Do not run Run All.
Do not modify stored data.

We have a mismatch between the current source code and the actual Trade Decision request generated after pressing Update.

The current source code in trade-decision-engine.ts clearly:

1. builds catalystTdeCandidates using buildCatalystTdeCandidates(...)
2. includes catalystTdeCandidates inside decisionProfile
3. adds a final task reminder:
   "Every entry in catalystTdeCandidates (...) must receive an explicit decision"

However, the actual OpenAI request generated by Update Trade Decision at approximately
2026-08-16 13:33 UTC does NOT contain catalystTdeCandidates inside BACKEND DECISION PROFILE.

It also does NOT contain the ticker-specific final reminder.

The request still contains Catalyst promotions only under the older:

"CATALYST INTELLIGENCE (priority 6.5 — non-actionable pre-event context...)"

section.

Therefore the running Trade Decision route appears not to match the current source code.

Please diagnose ONLY:

1. Confirm which exact compiled/runtime file handles:
   Update Trade Decision / the TDE API endpoint.

2. Confirm whether the running backend process contains the new
   catalystTdeCandidates code.

3. Confirm whether the deployed/built JavaScript contains these exact strings:
   - "catalystTdeCandidates"
   - "must receive an explicit decision"

4. Check whether the server is running stale compiled output or an old process.

5. Check whether a restart/redeploy/build step is required for the current
   TypeScript source to become active.

6. Check whether there are multiple Trade Decision routes/build artifacts and
   Update is hitting a different/older implementation.

7. Do NOT fix anything yet.

Return:
- exact route hit by Update Trade Decision
- exact runtime/compiled file serving it
- whether runtime contains catalystTdeCandidates
- whether runtime contains the explicit-decision reminder
- why the actual 13:33 prompt lacked both
- smallest action required to make the already-written code active

No OpenAI calls.
No code changes.
Read-only diagnosis only.